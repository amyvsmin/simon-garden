---
title: "指引詞（Pointer Word）"
slug: pointer-word
aliases: [指引詞, pointer word, 術語壓縮, 專有名詞當指令, 高密度術語, prompt 術語壓縮]
category: AI 與 Agent
confidence: 已驗證
created: 2026-07-23
---

## 定義
把 AI 訓練資料裡早就存在的專業術語，當成一個壓縮過的指令來用。你只寫一個詞，模型就展開成一整段它本來就懂的行為描述，省下大段解釋，也順便把發散的空間關掉。

## 關鍵面向
- **一個字換一百字**：對 AI 說「Data Clumps」，等於說了「檢查程式碼裡有沒有總是綁在一起出現的變數，有的話打包成一個獨立物件」。術語本身就是壓縮包，模型負責解壓。
- **前提是這個詞真的在訓練資料裡**：指引詞的威力來自「模型早就讀過幾千份談這個詞的文件」。自己發明的縮寫、公司內部黑話沒有這個效果，反而要多寫解釋。挑詞要挑有經典出處的（例如《Refactoring》的壞味道清單、設計模式名稱、資安框架術語）。
- **短不等於資訊少**：Matt Pocock 的 `grill-me` 只有五行字卻能持續發威，靠的就是每一句都是高密度指令、沒有一句廢話。字數少是結果，不是目標。
- **跟寫給人看的原則相反**：對人要避免行話（見[[curse-of-knowledge]]，專家常忘記別人不懂），對 AI 反而要主動用行話。兩種讀者的最佳寫法在這一點上是分岔的。

## 應用場景
- Simon 工作場景：寫或精簡 skill 時，把一整段行為描述換成既有術語——例如與其寫「派子代理去想辦法證明這個結論是錯的」，直接寫「跑一輪[[adversarial-verification]]／紅隊」；資安相關 skill 可以直接用 CISSP、MITRE ATT&CK 的標準名詞當指令。
- 一般場景：寫 prompt 時先問「這段話有沒有一個公認的名字」，有就用名字取代描述。

## 相關概念
- [[minimal-skill-writing]]：指引詞是那套精簡原則裡負責「壓縮」的那一條。
- [[curse-of-knowledge]]：對人是要翻譯掉的行話，對 AI 是要善用的壓縮包，正好相反。
- [[instructions-file]]：常駐指令檔特別吃字數，用指引詞能在同樣篇幅裡塞進更多約束。

## 尚未解決的疑問
- 一個術語在不同社群有不同定義時（例如「重構」在不同團隊指涉不同範圍），指引詞會不會反而放大歧義，影片沒談。

## 來源（自動維護）
- [[2026-07-23-matt-pocock-skills-teardown]]
